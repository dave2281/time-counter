<%= form_with(model: dailylog) do |form| %>
  <% if dailylog.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dailylog.errors.count, "error") %> prohibited this dailylog from being saved:</h2>

      <ul>
        <% dailylog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
