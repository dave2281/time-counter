<% content_for :title, "New Deed" %>

<h1 class="text-5xl font-semibold text-center text-blue-400 mb-6">New</h1>

<%= render "form", deed: @deed %>

<div class="mt-6 text-center">
  <%= link_to "Back to deeds", deeds_path, class: "text-blue-500 hover:text-blue-700" %>
</div>
