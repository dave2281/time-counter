<%= form_with(model: deed) do |form| %>
  <% if deed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deed.errors.count, "error") %> prohibited this deed from being saved:</h2>

      <ul>
        <% deed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
